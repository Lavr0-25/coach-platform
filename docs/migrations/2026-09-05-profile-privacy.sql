-- Приватность профиля (бэклог №19, этап 2)
-- is_public = false → профиль виден только владельцу и админу;
-- чужим показывается заглушка «Профиль скрыт».
-- По умолчанию профиль открыт (не ломаем существующих пользователей).

alter table profiles add column if not exists is_public boolean not null default true;

-- ===== 2. Уникальность подписки (фикс: двойной клик не должен создавать дубли) =====
-- Кнопка «Подписаться» теперь идёт через server action (проверка «уже подписан»
-- на сервере), но гонки двух параллельных запросов закрывает констрейнт в БД.
-- Применено 2026-09-05 (дублей в данных не было — проверено).

alter table subscriptions
  add constraint subscriptions_user_coach_uniq unique (user_id, coach_id);