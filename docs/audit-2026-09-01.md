# Аудит проекта coach-platform — 2026-09-01

> Статус: `stable` · Проверка read-only (код не менялся) · 4 направления: структура, маршруты, Supabase, качество кода

## Оценка состояния

**Хорошо:** типы чистые (`tsc` — 0 ошибок, strict включён), ключи не утекают (`.env.local` в `.gitignore`, service_role не используется), клиенты Supabase централизованы (2 фабрики, без самодельных), нет закомментированного кода и TODO-мусора.

**Главный системный риск:** реальная граница доверия — только RLS-политики Supabase. Код страниц ни владение контентом, ни роли толком не проверяет; ошибки БД в ~70 местах игнорируются. **RLS-политики надо ревизовать отдельно** (их содержимое из кода не видно — это следующий шаг проверки в Supabase-дашборде).

---

## 1. КРИТИЧНО — безопасность

| # | Проблема | Где | Суть |
|---|----------|-----|------|
| 1.1 | Фиктивная проверка владельца курса | `app/dashboard/mentor/courses/[id]/edit/page.tsx:96-110,166-198` | getUser/coach получены, но не используются: чужой ментор может редактировать любой курс (спасает только RLS) |
| 1.2 | Проверки владельца урока нет вообще | `app/dashboard/mentor/lessons/[id]/edit/page.tsx` | Ни auth.getUser(), ни фильтра по владельцу в update |
| 1.3 | Нет middleware (в Next 16 — proxy.ts) | корень проекта | Сессии в Server Components не обновляются → периодические «пропадания» логина на серверных рендерах |
| 1.4 | Восстановление пароля через implicit flow | `app/(auth)/reset-password/page.tsx:19` | Устаревший путь без `/auth/callback` и PKCE — менее безопасно |
| 1.5 | Модерация fail-open | `lib/banned-words.ts:12-15` | При ошибке БД фильтр стоп-слов **отключается** вместо блокировки |
| 1.6 | Клиент-only защита приватных страниц | `profile`, `courses`, `lessons`, `subscribers`, `analytics`, `favorites`, `notifications`, `feedback` | redirect после рендера — данные успевают уйти/показаться |
| 1.7 | `messages/layout.tsx` без auth-проверки | `app/messages/layout.tsx:8-13` | Серверный layout выполняет запрос до любого редиректа |
| 1.8 | RLS чтения admin-таблиц из браузера | `stop_list`, `system_settings`, `reports`, `feedback`, `banned_words` | Читаются любым залогиненным — зависит только от RLS (проверить политики) |
| 1.9 | Регистрация без emailRedirectTo; создание profiles/coaches завязано на DB-триггер | `app/(auth)/register/page.tsx:37` | Проверить наличие триггера `handle_new_user` в Supabase |

## 2. ЛОМАЕТ UX — битые маршруты и ссылки

| # | Проблема | Где |
|---|----------|-----|
| 2.1 | `/dashboard/mentor` — 404 (нет `page.tsx`), 11 ссылок + 4 redirect'а | `app/dashboard/mentor/` — хаб кабинета ментора мёртв |
| 2.2 | `/checkout` не существует — кнопки «Купить» в 404 | `app/course/[id]/page.tsx:256`, `app/lesson/[id]/page.tsx:263` |
| 2.3 | `/terms`, `/privacy` не существуют | `app/(auth)/register/page.tsx:219,223` |
| 2.4 | Конфликт бакетов `lesson-files` vs `lesson_files` | `components/FileUpload.tsx:72` (мёртв) vs `components/FileUploader.tsx:36` (живой) — вложения уроков могут не грузиться |
| 2.5 | Бакет `uploads` не из заявленного списка | `app/feedback/page.tsx:64` — загрузка скриншотов фидбэка может падать |
| 2.6 | Дубль профиля ментора: `/mentor/[id]` и `/profile/[id]` | Два живых маршрута одного и того же |
| 2.7 | Незавершённая миграция: `/mentor/analytics` не переехал в `/dashboard/mentor` | `app/mentor/analytics/page.tsx` |
| 2.8 | Миграция `/mentor/*` → `/dashboard/mentor/*` не завершена (удалён page.tsx, сирота 20 КБ) | git-история подтверждает |

## 3. ПОРЯДОК — мёртвый код и мусор

**Удалить (мусор):** `.npmrc#`, `app/.npmrc` (дубликат), `components/next.config.mjs` (конфликтует с корневым: `reactStrictMode:false`), `lib/hooks/` (пусто), `public/*.svg` (5 дефолтных), корневой `.npmrc` — UTF-16 с BOM (перекодировать в UTF-8).

**Сироты — не импортируются (~100 КБ):** `components/MentorDashboardClient.tsx` (не удалять до решения 2.1 — вероятно, это и есть удалённый дашборд), `CommentSection.tsx` (старая версия LessonComments), `AuthorProfile.tsx` + `AuthorProfileHeader.tsx`, `AnalyticsDashboard.tsx`, `LessonProgress.tsx`, `FileUpload.tsx`, `FeedbackModal.tsx`, `ProfileFilters.tsx`, `MessageModal.tsx`, `MessageButton.tsx`, `SignOutButton.tsx`, `app/lesson/[id]/FavoriteButton.tsx`, `app/dashboard/mentor/lessons/DeleteButton/`.

**Расхождения именования:** три «менторских» префикса (`/mentors`, `/mentor`, `/dashboard/mentor`); колокейшн-клиенты в `app/` vs PascalCase в `components/`; `FileUpload` vs `FileUploader`; `CommentSection.tsx` экспортирует `LessonComments`.

**Расхождение код ↔ БД:** таблица `notifications` не используется (уведомления собираются из комментариев); 13 таблиц, используемых в коде, отсутствуют в документации (comments, course_comments, stop_list, system_settings, review_reports, reports, lesson_content, banned_words, blocked_users, analytics_events, feedback, course_reviews); `course_reviews` — единственное упоминание в `app/page.tsx:241`, риск опечатки (рейтинг всегда 0).

## 4. КАЧЕСТВО КОДА

- **ESLint: 276 проблем (167 ошибок / 109 предупреждений) в 57 файлах.**
- `any` — 117 мест (типизация Supabase-ответов отключена; главный долг).
- Паттерн «load-функция объявлена ниже useEffect» — 26 ошибок `react-hooks/immutability` + 15 `set-state-in-effect`.
- 5 отладочных `console.log` с эмодзи в проде: `app/dashboard/mentor/profile/page.tsx:110,151,162,163,167`.
- 42 голых `<img>` вместо `next/image`, 39 неполных deps `useEffect`, 28 неиспользуемых переменных.
- `document.querySelector('form')?.getBoundingClientRect().top!` — возможный crash: `components/ReviewsSection.tsx:259`.
- Ошибки через `alert()` при наличии `components/Toast.tsx` — несогласованность.
- **Плюсы:** нет закомментированных кусков, нет TODO-мусора, `lib/` чистая от `any`, загрузчики файлов с корректной обработкой ошибок.

---

## Рекомендованный порядок работ

1. **Ревизия RLS в Supabase** (безопасность #1) — код доверяет только ей. Проверить: UPDATE coaches/courses/lessons по владельцу, чтение admin-таблиц, бакеты Storage.
2. **Хотфиксы UX:** восстановить `/dashboard/mentor` (из сироты MentorDashboardClient), временно скрыть/задизейблить кнопки «Купить» (нет checkout), страницы terms/privacy.
3. **middleware/proxy + auth callback** — сессии и PKCE.
4. **Проверки владельца** в edit-страницах (серверная проверка + фильтр по владельцу в каждом update).
5. **Чистка:** мусор + сироты (~100 КБ), унификация logout, console.log.
6. **Типизация:** типы Supabase вместо `any`, паттерн load-функций, `next/image`.

Открытые вопросы к владельцу: цель на 1–2 месяца (влияет на приоритеты 2 vs 5), конфликт бакетов (сверить фактический список в Supabase Storage), наличие триггера `handle_new_user`.