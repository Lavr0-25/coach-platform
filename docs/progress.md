# Прогресс работ — coach-platform

> Шапка: статус `stable` · обновлено 2026-09-04 (сессия 13) · ответственный: Анатолий + Claude Code

## Сессия 2026-09-04 (часть 4) — Справочник «Как здесь работать»: запушено 9d0b98b

### ✅ Итог (пуш 9d0b98b, 2026-09-04)

Фича «Техписатель» (бэклог №5) — **Справочник «Как здесь работать»**:

- ✅ **Иконка в подвале** всех страниц (`components/HelpLink.tsx` + `app/layout.tsx`): «Как здесь работать?» — передаёт адрес текущей страницы в `/help?из=…`.
- ✅ **Страница `/help`**: 11 разделов (кабинет, уроки, курсы, подписчики, аналитика, профиль, ИИ, обращения, админка, модерация, настройки интерфейса), чипы-оглавление, раздел по «из»/«раздел», HTML со скриншотами (не markdown — так решил Анатолий: «красивее»).
- ✅ **16 скриншотов** в `public/help/` (сняты агентом через Playwright).
- ✅ **Спека `docs/specs/help.md`** (stable) + **правило техписателя**: при любом изменении UI агент обновляет тексты разделов + переснимает скриншоты в том же коммите.
- ✅ Проверки: tsc 0 ошибок, консоль чистая, мобильный 375×812 без скролла, тёмная тема ок, заголовок виден (pt-24/28 под фиксированной шапкой).

### Точка входа в следующую сессию

1. **Домен myrightway.ru** — DNS-записи добавлены (A @ → 216.198.79.1, CNAME www → vercel), ждём делегирование; потом Refresh в Vercel Domains + проверка загрузки. Статус UNVERIFIED — идентификация через Госуслуги (напомнить Анатолию).
2. **№9** решение по РФ-хостингу / **№2** агентская часть ИИ-завода (промпты + расписание Windows).

### Хвосты

- `.mcp.json` — `read_only=true`; при следующей записи в БД: `/mcp` → supabase → Reconnect.

## Сессия 2026-09-04 (часть 3) — пауза на обед: план дня выполнен, всё запушено a546cb8

### 🔄 В работе (где остановились)

1. ✅ **Защита комментариев скрытых уроков** — SECURITY DEFINER `lesson_visible_to()` + политики SELECT/INSERT `comments`. Миграция `docs/migrations/2026-09-04-comments-hidden-lesson.sql` применена к прод-БД. Проверено: аноним/чужой — 0 строк, обычные уроки не задеты.
2. ✅ **Чистка тестовых данных прод-БД + демо-данные** — удалены тест-уроки, мусорные курсы, аккаунты tester4–8; созданы реалистичные демо-данные (авторы Анна/Михаил, курс, 4 текстовых урока, комментарии, отзывы, лайки). Реальный контент Дарины Богун не тронут. Проверено в браузере.
3. ✅ **Диагноз «сайт не открывается из РФ»** — тормозится доменное имя rightway.su (SNI в TLS); `coach-platform-pi.vercel.app` (тот же проект) из РФ открывается. Полный разбор — `docs/backlog.md` №4. Всё запушено: `a546cb8`.

### Точка входа в следующую сессию

1. **Решение по домену** (бэклог №4): паллиатив vercel.app / второй домен .ru на Vercel (рекомендация агента) / перенос в РФ (№9). Покупку домена делает Анатолий.
2. **№5 Техписатель** — собрать GCCEA (согласовано: pre-written тексты, попап-панель «Как здесь работать?» + файл-справочник).
3. **№2 агентская часть ИИ-завода** (промпты + расписание Windows).

### Хвосты

- `.mcp.json` — `read_only=true` восстановлен; при следующей записи в БД: `/mcp` → supabase → Reconnect.
- Dev-сервер (порт 3000) — запущен для просмотра демо-данных; погасить, когда просмотр закончен.

## Сессия 2026-09-04 (часть 2) — фича «Скрытые уроки»: завершена, запушено fc90cb9

### ✅ Итог (пуш fc90cb9, 2026-09-04)

Фича «Скрытые уроки» (бэклог №1) **завершена и запушена** (commit `fc90cb9`, 14 файлов):

- ✅ Страница урока (`app/lesson/[id]`): автодопуск по ссылке **до** чтения урока (RLS замкнутый круг + мемоизация fetch в Next — перечитывание не работает, перечитывание в generateMetadata требует смены URL `.limit(1)`); 404 для не-допущенных/отозванных; аноним → `/login?next=/lesson/<id>` (защита от open redirect: путь только `/…`, не `//…`).
- ✅ Исключение из публичных списков: главная (`app/page.tsx`), sitemap, профиль автора (`MentorProfile.tsx` — список + счётчик), счётчики `/mentors`. Проверено агентом в браузере (аноним + coach1): скрытый урок нигде не виден, консоль чистая.
- ✅ Спека `docs/specs/lessons.md` — раздел «Скрытые уроки»: режимы, RLS, доступ, «оплата обязательна» (допуск = право видеть страницу; контент — по обычным правилам), фильтры списков, границы (комментарии скрытого урока НЕ защищены RLS — отдельная задача).
- ✅ Тесты: ui-test 9/9 (аноним→login, автодопуск, БД-строка source=link, мобильный 375×812, отзыв→404, «только приглашения»→404, панель автора) + проверка каталога 5/5. Артефакты: `D:\Projects\tmp\ui-tests\2026-09-04-hidden-lessons\`.
- ✅ В тот же коммит: text-only формы уроков, блок цены, фикс Navbar `.maybeSingle()` (406 у пользователей без строки coaches).
- ✅ `read_only=true` в `.mcp.json` подтверждён (не трогали).

### Что дальше (бэклог)

1. **Защита комментариев скрытого урока** — RLS `comments` не проверяет видимость урока (осознанная граница, отдельная задача).
2. **Тестовые данные прод-БД**: урок b963385e остаётся is_hidden=true/link_access=true; допуски coach2/mentor/tester4-8 активны, coach3 отозван. Учесть при чистке тестовых аккаунтов.

### История сессии (до пуша)

UI кабинета и решения (панель внизу, тумблеры, поиск по coaches.display_name):

1. ✅ **Миграции БД применены (прод, через MCP)**:
   - `hidden_lessons`: `lessons.is_hidden` + `lessons.link_access`, таблица `lesson_access` (unique(lesson_id,user_id), source manual/link, revoked-отзыв без удаления), RLS-политики, пересоздана `lessons_public_read` (скрытый урок читают только автор и допущенные). Файл: `docs/migrations/2026-09-04-hidden-lessons.sql`.
   - **Фикс рекурсии RLS** (`hidden_lessons_recursion_fix`): первые политики lesson_access ссылались на lessons напрямую → «infinite recursion» → **все запросы уроков падали с 500**. Чин: проверки через SECURITY DEFINER-функции `is_coach_of_lesson()` и `lesson_accepts_link()`.
   - `course_lessons_lesson_coach_delete`: автор урока может отвязать его от чужого курса (нужно для «скрытый урок всегда самостоятельный»).
2. ✅ **Серверные экшены** `app/actions/hiddenLesson.ts`: setLessonHidden (проверка контента, отвязка от курса через удаление course_lessons, is_published=true), setLessonLinkAccess, addLessonAccess (восстановление отозванной строки), revokeLessonAccess (revoked=true).
3. ✅ **UI кабинета** (показано Анатолию, одобрено): `components/HiddenLessonPanel.tsx` + подключение на страницу редактирования урока. Итоговый вид после правок:
   - Панель **внизу** страницы, сразу после «Цена и доступ» (просил Анатолий); наверху только компактный статус «🔒 Урок скрыт» (в скрытом режиме).
   - **Переключатели-тумблеры** (role="switch"), не кнопки (просил Анатолий): скрытый режим + приём по ссылке.
   - Поиск людей: **профили (имя+email) И публичное имя автора (coaches.display_name)** — слияние без дублей; в списках показывается сначала публичное имя автора. Причина правки: «иван» не находилось — имя живёт в coaches, не в profiles.
   - Список допущенных: аватар, имя, email, пометка «по ссылке»/«вручную», кнопка «Отозвать», свёрнутый блок «Отозванные: N».
4. ✅ **Схема БД (важно!)**: у `lessons` НЕТ `course_id` (связь через `course_lessons`); у `profiles` ключ — `id` (совпадает с auth.users.id), колонки `user_id` нет. На этом ловили две ошибки.

### Точка входа в следующую сессию (задача #6)

1. **Страница урока** `app/lesson/[id]/page.tsx`: автодобавление по ссылке (не допущен + link_access=true → insert lesson_access source='link', RLS самоприглашение уже в БД), 404 для не-допущенных (RLS уже даёт — остаётся проверить), для анонима → редирект на /login.
2. **Исключение скрытых из каталога/поиска/sitemap/профиля автора** (фильтр `is_hidden=true`).
3. **Спека** `docs/specs/lessons.md` — раздел «Скрытые уроки» (код+спека одним набором!).
4. **Тесты**: по ссылке (допуск + пометка), отзыв → 404 и повторный вход по ссылке НЕ возвращает доступ, режим «только приглашения», мобильный вид.
5. **Коммит** (только с ОК Анатолия): text-only формы + блок цены + скрытые уроки + спека + миграции + .mcp.json.
6. После фичи: вернуть `read_only=true` в `.mcp.json`, обновить чекпоинт.
7. Хвосты: удалить `C:\matchself` (ждёт ОК); тестовое состояние прод-БД: урок b963385e «💰 JavaScript…» сейчас is_hidden=true, отозван coach3, отвязан от курса.

### Решения сессии

- Отзыв доступа = revoked=true, строка не удаляется (unique блокирует повторный самозахват по ссылке).
- Скрытый урок всегда самостоятельный: при включении режима урок удаляется из всех курсов (course_lessons), автор может отвязать от чужого курса.
- Режим «только по приглашению» = link_access=false (ссылка не открывает).

## Сессия 2026-09-04 — навигация кабинета ментора + хвосты сессии 10

### Сделано

1. ✅ **Единая навигация кабинета** — новый `components/MentorSectionNav.tsx` («Кабинет · Мои курсы · Подписчики · Управление с ИИ · Аналитика · Профиль»), подключён на 12 страницах менторского раздела. Активный раздел подсвечен (aria-current, фиолетовое подчёркивание); «Кабинет» — только точное совпадение, остальные — по префиксу адреса.
2. ✅ **Убраны дубли навигации**: «Назад в кабинет» (уроки), «Назад» (редактор курса), хлебные крошки на внутренних страницах, «← В кабинет» (ИИ-хаб), «← К разделу…» (план тем, ключи), вкладки «Профиль / Аналитика» в профиле. Остались: пагинация «← Назад» (подписчики) и «Назад к профилю» (внутри вкладки настроек) — это не навигация между разделами.
3. ✅ **Меню пользователя в шапке** (`Navbar.tsx`): убраны дубли «Мои курсы» и «Управление с ИИ»; «Личный кабинет» теперь ведёт на главную кабинета (`/dashboard/mentor`, иконка домика), а не на профиль.
4. ✅ **Главная кабинета**: строка навигации поднята в самый верх; убраны надпись «КАБИНЕТ» и строка «N уроков · N курсов · N подписчиков» (дубли плиток статистики).
5. ✅ **`read_only=true` возвращён в `.mcp.json`** (после SQL-конверсии 2026-09-03).
6. ✅ Проверки: `tsc --noEmit` 0 ошибок; подсветка активного раздела проверена агентом в браузере на 5 страницах (ровно один активный пункт).

### Дальше

1. **UI-правки «только текст»**: CONTENT_TYPES — оставить только text в `app/dashboard/mentor/lessons/new/page.tsx` и `[id]/edit/page.tsx`; обновить спеки `docs/specs/lessons.md` + `agent-api.md` (код+спека одним набором); показать в браузере.
2. ⚠️ Хвост: старая копия проекта в `C:\matchself` (порт 3000) — предложено удалить, ждёт ОК Анатолия.

## Сессия 2026-09-03 (часть 4) — бэклог: «только текстовые уроки» (SQL выполнен, UI проверен)

### 🔄 В работе

1. ✅ **Фикс 406** — `components/LessonProgress.tsx`: `.single()` → `.maybeSingle()`. tsc 0 ошибок. (Закоммичено в сессии 11.)
2. ✅ **`docs/backlog.md`** приведён в порядок. (Закоммичено в сессии 11.)
3. ✅ **SQL-конверсия «только текстовые уроки» — ВЫПОЛНЕНА** (MCP read_only=false, ОК Анатолия):
   - Удалено 6 мусорных уроков, сконвертировано 9 (8 из плана + «Брюшной мышечный блок», добавлен отдельным UPDATE). В БД **10 уроков, все `content_type='text'`**, видео-типов нет.
   - Тексты сгенерированы агентом (Райх/практики для уроков Дарины), HTML в `lesson_content.content_html`.
4. ✅ **UI-тест 6/6 ✅** (Playwright, отчёт: `D:\Projects\tmp\ui-tests\2026-09-03-text-lessons\report.html`):
   - Рендер текстов, тёмная тема, мобильный 375×812, панель автора, каталог курсов — всё в порядке.
   - **Диагностика «ошибок» Анатолия:** порт 3000 занимала старая копия из `C:\matchself` (всё, кроме главной — 404); правильный dev-сервер (порт **3001**) деградировал (Turbopack-воркеры падали, уроки 500) — **перезапуск исправил**. Сейчас сервер на 3001 запущен из фонового процесса агента.

### Точка входа в следующую сессию

1. **Вернуть `read_only=true`** в `.mcp.json` (сейчас false после SQL-конверсии).
2. **Закоммитить:** фикс 406 (LessonProgress.tsx) + docs/backlog.md + docs/progress.md. Решить про `.mcp.json`: коммитить с `read_only=true`.
3. **UI-правки «только текст»:** убрать не-текстовые типы из `app/dashboard/mentor/lessons/new/page.tsx` и `[id]/edit/page.tsx` (CONTENT_TYPES — оставить text), обновить спеки `docs/specs/lessons.md` / `agent-api.md` (правило: код+спека одним набором), показать в браузере.
4. ⚠️ Хвост: старая копия проекта в `C:\matchself` (порт 3000) — предложено удалить, ждёт ОК Анатолия (диск C беречь).

### Контекст решений

- БД до чистки: 16 уроков, 15 не-текстовых (тестовые). Авторы: coach1/coach2@test.ru (известные), mentor@test.com (пароль в memory), d.v.bogun@gmail.com — **жена Анатолия, помогала тестировать** (её 4 психологических урока — самый «боевой» контент, конвертируем в текст с сохранением авторства).
- «Нарядность» (разнообразие уроков по авторам) сохраняется: после конверсии 9 текстовых уроков у 3 авторов.

## Сессия 2026-09-03 (часть 1) — MCP-сервер Supabase (ДЗ курса)

### Сделано

1. Playwright 1.62.1 установлен **глобально** (npm -g) + Chromium скачан в `%LOCALAPPDATA%\ms-playwright` — для будущих e2e-тестов; до этого у агента только встроенный Playwright MCP в сессии.
2. Выполнение ДЗ курса («подключи MCP-сервер проектно»): выбран **Supabase MCP** — закрывает главную слепую зону агента (нет прямого доступа к БД; SQL Анатолий гоняет руками через SQL Editor). Альтернативы (Vercel MCP, Context7) отклонены: редкая нужда / дублируют WebSearch.
3. Установлен проектно через `claude mcp add --transport http supabase "https://mcp.supabase.com/mcp?project_ref=ftokvvzgvzkphszgfjbi&read_only=true&features=database,docs" --scope project` → создан **`.mcp.json`** в корне (без секретов — можно коммитить). Chек-лист ДЗ: автор supabase-community (официальный, Apache 2.0), ~425 коммитов, OAuth вместо API-ключа, read_only, дублей нет.

### Точка входа в следующую сессию

~~Анатолий перезагружает Claude Code → в новой сессии: `/mcp` → Authenticate (OAuth Supabase в браузере) → скриншот статуса connected (артефакт ДЗ №1).~~
~~Потом: первый реальный запрос агенту через MCP (проверить данные БД — таблицы, последние обращения фидбека) → скриншот результата.~~
~~Потом: `/context` → скриншот расхода токенов MCP-сервером.~~
~~Затем **текстовый отчёт ДЗ** — план: задача (прямой доступ агента к Supabase вместо ручного SQL Editor), сервер (Supabase MCP), почему (официальный автор, OAuth без хранения ключа, read-only для безопасности, не дублирует ничего), функции (SQL-запросы к БД, чтение таблиц), первый результат, польза проекту.~~

## Сессия 2026-09-03 (часть 2) — ДЗ завершено: OAuth, первый запрос, отчёт

### Сделано

1. **OAuth-авторизация Supabase MCP** пройдена (`/mcp` → Authenticate → ссылка в браузере → Authorize). Статус: `supabase · connected · 5 tools`. Скриншот: `D:\Projects\tmp\dz\2026-09-03_00-32-32.png`.
2. **Первый реальный запрос через MCP**: `list_tables` — 39 таблиц в public, RLS включён во всех; `execute_sql` — 5 последних обращений фидбека (id, дата, статус, тип, тема). Показательный момент: первый SQL содержал несуществующую колонку `category` → ошибка `42703` → агент посмотрел структуру таблицы через MCP (колонка называется `type`) и исправил запрос сам. Иллюстрация: `D:\Projects\tmp\dz\03-mcp-first-query.png` (+ исходник `mcp-query-result.html`).
3. **`/context`** — MCP tools: 3.9k токенов (1.9%) из 200k; 29 инструментов (5 Supabase + 24 Playwright). Скриншот: `D:\Projects\tmp\dz\2026-09-03_00-24-05.png`.
4. **Текстовый отчёт ДЗ** — `D:\Projects\tmp\dz\отчёт-ДЗ-mcp-supabase.md` (задача / сервер / почему / функции / первый результат / цена / польза). ДЗ курса выполнено полностью.

### Точка входа в следующую сессию

- ⚠️ Остатки с прошлых сессий: тест не-админского ключа агентского API не сделан; Next 16.3.4. (AGENT_KEY из Vercel удалён — Анатолий подтвердил 2026-09-03, делал раньше.)

## Сессия 2026-09-03 (часть 3) — две фичи + скилл ui-test

### Сделано

1. **Скилл `ui-test`** (`C:\Users\Lavr\.claude\skills\ui-test\`): агент сам проверяет UI — тест-кейс на согласование → прогон Playwright MCP → report.md + **report.html** (скриншоты base64, открывается `start`). Артефакты: `D:\Projects\tmp\ui-tests\<дата-тема>\`. Тестовые аккаунты — в memory.
2. **«Не публиковать урок без контента»** — закрыт пункт бэклога: серверная проверка в `setLessonPublished` + disabled-кнопка в UI. Протестировано скиллом ui-test (7/8). Коммит `1a6b7d6` — на проде.
3. **«Наставник удаляет свой урок»** — находка из теста №1 (в кабинете не было удаления): `deleteLesson.ts` + красная кнопка на странице редактирования; каскад чистит 13 дочерних таблиц; файлы Storage остаются (граница в спеке). Протестировано (4/4). Коммит `89aa41e` — на проде.
4. **Инцидент:** долгоживущий dev-сервер отдал 500 на `/lessons/*/edit` («Jest worker…» в логах был и до правки) — лечится перезапуском; записано в скилл ui-test как первая проверка при 500.
5. Чекпоинты: `checkpoint-2026-09-03-supabase-mcp` (обновлён), `ui-test-skill`, `test-accounts-coach-platform`, `dz-screenshots-folder` в memory.

### Точка входа в следующую сессию

- Хвосты: Next 16.3.4; «индексация для ИИ-агентов» (фича из фидбека, in_progress). Закрыты 2026-09-03 (утро): тест не-админского ключа (7/7, `D:\Projects\tmp\ui-tests\2026-09-03-agent-key-nonadmin\`), AGENT_KEY из Vercel (удалён Анатолием ранее).
- Идеи в бэклог из находок: чистка осиротевших файлов Storage; П4 ui-компоненты.

## Сессия 2026-09-02 (часть 9) — WYSIWYG-редактор текстовых уроков

### Сделано

1. **Текстовый урок «как в Дзене»** (обращение пользователя): новый тип контента `text` в Tiptap 3. Редактор `components/editor/RichTextEditor.tsx`, расширения в одном источнике `lib/editor/lessonExtensions.ts` (StarterKit, Image, Youtube, TextAlign, своё VK-расширение). Блоки: H1–H3, Ж/К/Ч, списки, цитата, разделитель, выравнивание, картинки в Storage, YouTube, VK. Sticky-панель под шапкой.
2. **Безопасность — санитайзер из самого Tiptap** (`lib/editor/sanitizeLessonHtml.ts`, сервер): HTML парсится в JSON по схеме редактора → собирается обратно; всё вне схемы отбрасывается. Двойной замок: при сохранении (server action) и при выводе (server component). Тест: script/onclick/javascript:/чужой iframe — удаляются.
3. **БД**: колонка `lesson_content.content_html` + `'text'` в CHECK-ограничении (миграция `docs/migrations/2026-09-02-wysiwyg-text-lessons.sql`, выполнена через exec_sql). Урок-урок: при новом типе контента править и код, и CHECK в БД — падало сохранение.
4. **Создание урока**: тип «Текст» ведёт сразу в редактор; `lesson_content` создаётся при первом сохранении.
5. **Просмотр**: `app/lesson/[id]` рендерит очищенный HTML в `.lesson-prose`; стили на токенах темы (тёмная тема/палитры работают), мобильная типографика.
6. **Спека `docs/specs/lessons.md`** (создана): модель, типы, очистка, приёмочные, границы.
7. Бэклог: «не публиковать урок без контента», «обновить Next до 16.3.4» (7 high npm audit — старые, не от Tiptap).

### По пути исправлено

- Sticky-панель «уезжала»: `overflow-hidden` контейнера ломает sticky — убран.
- Ошибка сохранения не была видна (плашка за экраном на длинной странице) — авто-прокрутка к ошибке.
- Урок сохранялся/публиковался, а контент падал про CHECK → «студенту недоступен».

### Открытое

- **Точка входа в следующую сессию**: рекомендованный следующий шаг — мелкий пункт бэклага «не публиковать урок без контента» (15 минут), затем тест не-админского ключа и обновление Next до 16.3.4. Обновление Next — отдельным шагом с регресс-проходом.
- Приёмка №2 (мобильный + тёмная тема на просмотре) — «пока норм» от Анатолия; прод-проверка текстового урока на rightway.su после автодеплоя ca48c3d.
- AGENT_KEY из Vercel не удалён (не подтверждено).

## Сессия 2026-09-02 (часть 8) — ответы пользователям + фикс RLS feedback

### Сделано

1. **Ответ поддержки на обращения** (фича «Ответ пользователям + возврат на доработку»): в feedback добавлены поля `admin_reply` + `replied_at` (миграция через exec_sql — агент делает SQL сам); PATCH агентского API принимает необязательный `reply` (пустой — стирает ответ); админка /admin/feedback — textarea «Ответ пользователю» в модалке статуса; /feedback «Мои обращения» — фиолетовая плашка «Ответ поддержки · дата». Возврат на доработку = статус «Новое» + комментарий «Недостаточно данных: …» (обращение снова редактируемо пользователем).
2. **Исправлена логика удаления (и правки) новых сообщений**: причина не в коде — RLS-политики feedback из faa51bb были неполные (DELETE не было вовсе, UPDATE был только админский). Добавлены 3 политики: пользователь правит/удаляет СВОЁ, пока «Новое»; админ удаляет любые. Полный набор — 7 политик, зафиксирован в спеке agent-api.md (вместе с уроком: проверять все 4 команды при включении RLS).
3. **Бэклог дополнен**: аудит RLS-политик остальных таблиц (техдолг), перенос данных в РФ 152-ФЗ (дорожная карта).

### Проверено

- Плашка «Ответ поддержки · 2 сентября» с текстом — видна пользователю; удаление/правка своих обращений в статусе «Новое» — работает (проверил Анатолий).
- PATCH через API с reply — ok; PATCH с неверным статусом — 400.

## Сессия 2026-09-02 (часть 7) — ключи агентского API в базе + развилка по роли

### Сделано

1. **Загадка «пустого feedback» решена**: записи были всегда; слепым было чтение анонимным клиентом — на `feedback` работает RLS (включена в faa51bb), и неавторизованные запросы (агентское API, curl) получали пустоту. Админка в браузере видела всё (сессия пользователя).
2. **Ключи агентского API переехали в базу** (вариант «В», выбран Анатолием): таблица `agent_keys` (хэш sha256 вместо ключа, user_id, имя, last_used_at, revoked_at; RLS без политик — только сервисный клиент), страница **`/api-keys`** для всех пользователей (создать — виден один раз, список своих, отзыв; ссылка в меню профиля), server actions `app/actions/agentKeyActions.ts`.
3. **Развилка по роли** (lib/agentAuth.ts возвращает роль): ключ админа — полный доступ; ключ обычного пользователя — ТОЛЬКО чтение своих обращений (PATCH/жалобы → 403); в ответе feedback поле `scope`.
4. `lib/supabase/admin.ts` — сервисный клиент (SUPABASE_SERVICE_ROLE_KEY в `.env.local` + Vercel Secret/Production, Redeploy выполнен пользователем).
5. Страница `/admin/agent-keys` удалена (замена: `/api-keys`); ToastProvider обёрнут локально; ошибка React на голый `<script>` темы исправлена по гайду preventing-flash-before-hydration (type=text/javascript|text/plain + suppressHydrationWarning в `<head>`).
6. Спека `docs/specs/agent-api.md` и скилл `agent-admin-api` переписаны под схему «ключи в базе».
7. Закоммичено и запушено `6eb701a` — API проверен на проде (rightway.su): работает.
8. Обратная связь: разбор 5 новых обращений, статусы выставлены (баг формы «Решено», идеи про ИИ — «В работе»). **Баг формы фидбека исправлен** (форма больше не блокируется после отправки) — ⚠️ фикс в app/feedback/page.tsx пока НЕ закоммичен.
9. Создан **`docs/backlog.md`** — единый бэклог (в работе / согласовано / дорожная карта / техдолг); правила: идея попадает после согласования Анатолия.

### Проверено

- API: без ключа 401; с админским ключом — 7 обращений (`scope: "admin"`); жалобы — 16, сводка «кто на кого» строится; PATCH с неверным статусом 400. tsc — 0 ошибок.
- Развилка не-админ (чтение только своих, 403 на запись) — код есть, живого теста не-админским ключом ещё не было.

## Сессия 2026-09-02 (часть 6) — П5 админка добита + агентское API

### Сделано

1. **П5 закрыт**: градиентные заливки → белый стиль во всех подстраницах админки (users: StatCard+шапка списка, feedback: StatCard+thead+шапка модалки, settings: инфо-блок, banned-words: 2 блока, reports: цитата). Семантика (бейджи, красные кнопки, аватары, скелетоны) сохранена.
2. **/admin/lessons + /admin/coaches переделаны в текущий стиль** (были «старые»: синие кнопки, rounded-lg, без крошек): Breadcrumbs + «← Назад», белые карточки, чипы-рамки, gradient-btn, современные модалки, toast вместо alert, аватары у наставников. Breadcrumbs знает coaches/lessons.
3. **Агентское API** (`/api/agent/*`, спека `docs/specs/agent-api.md`): feedback GET+PATCH, reports GET (сводка «кто на кого»)+DELETE, ключ AGENT_KEY через x-agent-key (lib/agentAuth.ts). Банов в API нет — ступень 2 (методика модерации).
4. **Скилл `agent-admin-api`** (глобальный, C:\Users\Lavr\.claude\skills\agent-admin-api\SKILL.md) — паттерн для всех проектов: обнаружение ключа, рабочий цикл, правила безопасности, шаблон внедрения.
5. AGENT_KEY добавлен в Vercel (Secret, Production) + Redeploy — выполнено пользователем.

### Открытое

- ~~Проверка API на проде после пуша~~ / «новое предложение не видно» — закрыты в части 7 (RLS-загадка решена).
- Переменную `AGENT_KEY` из Vercel можно удалить (ключи теперь в базе; Redeploy при удалении).

## Сессия 2026-09-02 (часть 5) — цикл работы с обращениями (фидбек)

### Сделано

1. **Лимит скриншотов** (`/feedback`): multiple-загрузка, до 5 файлов на обращение (MAX_IMAGES), неподходящие файлы пропускаются с сообщением, остальные грузятся.
2. **«Мои обращения»** (`/feedback`): история своих обращений под формой — тип, заголовок, статус-бейдж (как в админке), дата ru-RU, описание, миниатюры скринов (клик — новая вкладка). Редактирование (форма переиспользуется, режим правки с подсказкой и «Отменить правку») и удаление (два клика, без нативных диалогов) — только пока статус «Новое». Авто-редирект на главную после отправки убран.
3. **Server actions** (`app/actions/feedbackActions.ts`): updateMyFeedback / deleteMyFeedback — проверка владельца + статус «Новое» прямо в запросе (.eq user_id + .eq status), при удалении скриншоты вычищаются из Storage (путь извлекается из публичного URL).
4. **Массовые действия в админке** (`/admin/feedback`): чекбоксы в таблице и мобильных карточках, «выбрать все» по отфильтрованному списку, панель «Выбрано: N → В работе / Решено / Отклонено / Снять», один запрос .in('id', [...]). Выделение сбрасывается при смене фильтра/поиска.
5. **Выгрузка по фильтру**: «Выгрузить JSON (по фильтру)» и CSV экспортируют отфильтрованный набор (фильтр «Новое» → только новые). Кнопка показывает «(по фильтру)», когда фильтр/поиск активны.

### Рабочий цикл обращений (схема)

Отправил → видит в «Моих обращениях» → правит/удаляет, пока «Новое» → админ выгружает JSON по фильтру «Новое» → `scrin_bag/` → агент качает скрины, отрабатывает → админ массово: «В работе» → после разбора «Решено»/«Отклонено».

### Дальше

1. **WYSIWYG-редактор текстовых уроков** (обращение «как в Дзене») — принято в бэклог, спланировать по GCCEA (кандидат: Tiptap vs своё).
2. П5 остаток: подстраницы админки (~150 цветных вкраплений).
3. П4: ui-компоненты (Toast вместо 82 alert(), Button/Card/Badge/Input), lucide-react; перенос админских записей (feedback, статусы) в server actions.
4. Отложено: язык интерфейса (этап 3), ~30 .single(), платежи, storage SELECT-политика, SSR главной.

## Сессия 2026-09-02 (часть 4) — единый стиль, настройки интерфейса, оптимизация

### Сделано (всё запушено, автодеплой на rightway.su)

1. **Токены стиля + палитры** (`d3d0d17`): globals.css переписан на `@theme inline` + color-mix — шкалы Tailwind (gray/purple/blue/green/red...) замаплены на переменные палитр; 4 палитры (Фиолет/Хвоя/Чернила/Графит), тёмная тема, размер шрифта (85–130%), Manrope с кириллицей вместо Geist, ребрендинг CoachPlatform → RightWay.
2. **ThemeSettings**: настройки интерфейса (палитра/тема/шрифт/язык-«скоро»); анонимам — шестерёнка в шапке, зарегистрированным — пункт в меню профиля; anti-flash inline-скрипт в layout; suppressHydrationWarning.
3. **П5** (`e867a39`): админка — StatCard/MiniStatCard без цветных заливок (белый стиль, цвет только в цифре/иконке), AdminLink на gradient-icon, hover:scale убраны.
4. **П6** (`e867a39`): скриптом по 25 файлам — 19 hover:scale удалено, 24 transition-all → transition-colors (на элементах без движения); иконки колокольчиков унифицированы.
5. **Фиксы сердечка** (`e867a39`): lesson + course pages — FavoriteButton из absolute в flex-ряд статистики (перекрывал бейдж цены).
6. **next/image** (`8ac7287`): 25 картинок в 13 файлах переведены на Image (fill+sizes для обложек, width/height для аватарок); next.config remotePatterns для supabase.co; uploaders (3 шт) не тронуты — blob-превью.
7. **Фикс N+1 на /mentors** (`8ac7287`): было 2N+1 запросов (по каждому автору — counts курсов/уроков), стало 3 запроса + группировка через Map. Замер: 1,1 с тёплая dev-загрузка.

### ⚠️ Незакрытое

- **Скрин в репо:** `scrin_bag/2026-09-02_16-03-43.png` случайно попал в коммит `8ac7287` (git add -A). Предложено: удалить файл + .gitignore для scrin_bag (кроме .gitkeep), отдельный коммит. **Ждёт ОК.**

### Дальше

1. Fixup-коммит скрина (после ОК).
2. П6 остаток: 129 transition-all на элементах с движением — ручная аккуратная работа.
3. П5 остаток: подстраницы админки (~150 цветных вкраплений).
4. П4: ui-компоненты (Toast вместо 82 alert(), Button/Card/Badge/Input), lucide-react.
5. Отложено: язык интерфейса (этап 3), ~30 .single() по проекту, платежи, storage SELECT-политика, SSR главной.

## Сессия 2026-09-02 (часть 3) — восстановлен /dashboard/mentor

### Сделано

1. **Проверки сессии 4 закрыты** (браузер + HTTP-уровень): /terms, /privacy, футер, кнопки «Купить» (disabled + 0 ссылок на /checkout), многие-ко-многим (уроки в 2–3 курсах), RLS-сценарии, tsc 0 ошибок.
2. **Восстановлен `/dashboard/mentor` ✅** — создан `app/dashboard/mentor/page.tsx` (серверный компонент): getUser → redirect /login, coach через `.maybeSingle()`, загрузка данных (уроки наставника, favorites с вложенными уроками, lesson_progress со status 'started'/'completed', purchases payment_status='completed'); заголовок «Кабинет автора» + быстрые ссылки (уроки/курсы/профиль/подписчики/аналитика).
3. **`MentorDashboardClient.tsx` доработан:** заполнены потерянные иконки (📚 🎓 📝 🗂️ 💳).
4. **Решение продукта: все пользователи — авторы с первого дня.** Подтверждено данными: 8 profiles = 8 coaches, у всех есть coaches-строка (триггер handle_new_user работает). Заглушка «Вы ещё не автор» и флаг isCoach — убраны; страховка: если coaches-строки нет (аномалия) — страница создаёт её сама при первом визите (insert с display_name из user_metadata/email).
5. Схема подтверждена REST-опросом: `lesson_progress` (status/progress_percentage/last_watched_at), `purchases` (purchased_at/amount/payment_status), `favorites` (group_name) — все колонки, что ждёт компонент, существуют. tsc — 0 ошибок.

### Где остановились (чекпоинт сессии 5)

- **Вход в следующий шаг:** коммит + push текущих правок (текст коммита предложен, ждал ОК) → потом финальные пункты запуска: адаптивность ключевых страниц на проде (мобильный viewport) и выбор платёжного сервиса.
- **Не делаем:** SSR главной (#8 — отложено до продвижения), замороженные фичи (тесты/подписки/12 таблиц без политик), дроп мёртвых таблиц.
- **Открытые вопросы:** бэклог безопасности — широкая SELECT-политика на storage.objects («Clients can list all files»); платёжный сервис не выбран; `handle_new_user`-триггер существует (проверено: profiles=coaches 8:8).
- Все проверки пользователем в браузере пройдены: кабинет, 404, редирект профиля, прогресс урока (включая фикс скелетона для анонима), RLS-сценарии.

### Чистка мёртвого кода (сессия 5, по аудиту 2026-09-01)

- Удалены 12 сирот-компонентов/папок (CommentSection, AuthorProfile(+Header), AnalyticsDashboard, FileUpload (живой — FileUploader), FeedbackModal, ProfileFilters, MessageModal, MessageButton, SignOutButton, app/lesson/[id]/FavoriteButton, lessons/DeleteButton/) + мусор (.npmrc#, app/.npmrc, components/next.config.mjs, lib/hooks/, public/*.svg).
- `.npmrc` перекодирован UTF-16 → UTF-8 (npm теперь читает legacy-peer-deps, пропали мусорные warn).
- tsc — 0 ошибок после чистки.
- **Дроп `lessons.course_id` ✅** (`docs/migrations/2026-09-02-drop-lessons-course-id.sql`, v2): первая попытка упёрлась в зависимость — политика `lessons_public_read` ссылалась на колонку; пересоздана через `course_lessons` (урок опубликован сам ИЛИ входит в опубликованный курс), потом дроп. Проверено REST: колонка исчезла, опубликованное читается, черновики скрыты.
- `dogfood-output/` удалена (была в .gitignore, не коммитилась).
- **Конфликт бакетов ✅** — удалены мёртвые `lesson-files` (1 сиротский файл, БД не ссылалась) и `feedback-files` (пуст; код пишет в `uploads`). Осталось 4 живых: `uploads`, `avatars`, `lesson_files`, `covers`. SQL-удаление storage-таблиц Supabase блокирует триггером `storage.protect_delete` — только через API/интерфейс (Edit bucket ▾ → Delete bucket).
- **Бэклог безопасности:** предупреждение Supabase «Clients can list all files in this bucket» — широкая SELECT-политика на `storage.objects` у публичных бакетов; проверить перед запуском.
- **⚠️ Найдено по ходу:** `components/LessonProgress.tsx` — единственный писатель `lesson_progress`; сирота → прогресс обучения нигде не записывается. **Решение: вернуть в страницу урока ✅** — подключён к `app/lesson/[id]/page.tsx` (после блока контента; показывается только тем, кто видит контент, анониму скрывается сам). Улучшения: upsert теперь задаёт started_at/completed_at явно (страховка от NOT NULL), кнопки переведены на gradient-btn. Показан в браузере пользователем — статус урока меняется (не начат → в процессе → завершён), кабинет видит «В процессе/Завершено».

### Dogfood #5–#7 (сессия 5)

1. **#5 ✅** — дубль профилей закрыт: `/profile/[id]` стал тонким редиректом на канонический `/mentor/[id]` (богатый клиентский профиль, на него ведёт каталог /mentors). id резолвится двумя запросами: сначала user_id (комментарии/отзывы), потом coaches.id. Мёртвый `ProfileFilters.tsx` — в список на чистку (не используется нигде).
2. **#6 ✅** — `app/messages/layout.tsx`: серверная проверка `getUser()` → redirect /login для анонима.
3. **#7 ✅** — создан `app/not-found.tsx` (брендированная 404: 🧭, gradient-текст, кнопки «На главную» и «Все авторы»).
4. **#8 — отложено:** SSR главной пустая ('use client'); рекомендация отчёта — переносить в server-компонент только при продвижении. Возвращаемся к этому при выборе «запуск с минимумом».
5. **#9 — открыто:** тестовый урок «тест картинки» в публичном курсе 259691b4 — удалить через SQL по ОК пользователя.

## Сессия 2026-09-02 (часть 2) — стиль, защита, course_lessons (уроки в нескольких курсах)

### Сделано

1. **Единый стиль:** футер (layout.tsx), формы `/login` и `/register` переведены на фиолетовую палитру сайта (gradient-btn, purple-100 и т.д.).
2. **Пункт 3 плана безопасности ✅ — server actions:** созданы `app/actions/updateCourse.ts` (updateCourse, attachLessonToCourse, detachLessonFromCourse, reorderCourseLessons) и `app/actions/updateLesson.ts`. Проверка владельца на сервере: `getUser()` → coach → `.eq('coach_id', ...)` в самом запросе + проверка затронутых строк через `.select('id')` (ловит чужие id и RLS-тихий отказ). Edit-страницы больше не пишут в БД из браузера.
3. **Dogfood #2 ✅** — `proxy.ts`: PROTECTED_PREFIXES + серверный 307 на /login для анонима (проверено пользователем).
4. **Фиксы 406:** `.single()` → `.maybeSingle()` в edit-страницах (курс/урок не найден → понятное сообщение) и в запросе покупок на публичной странице курса.
5. **Многие-ко-многим уроки↔курсы ✅:** миграция `docs/migrations/2026-09-02-course-lessons.sql` (таблица `course_lessons`, перенос связей, обнуление `lessons.course_id`); RLS на course_lessons выключен (вся база пока без RLS, политики — следующий шаг). Код переключён: server actions, edit-страница курса, публичная страница курса, «Мои уроки» (бейдж курсов, `+N` если несколько), публичная страница наставника, статистика профиля, lessons/new. tsc — 0 ошибок.
6. **Новое правило пользователя:** инструкции всегда полным текстом (ссылка + скрипт целиком, без отсылок «см. выше») — записано в глобальный CLAUDE.md (п.1c) и память.

### Где остановились

- ✅ Проверки пользователем пройдены (2026-09-02, сессия 5): /terms, /privacy, футер, кнопки «Купить», сценарий «один урок в двух курсах», RLS-сценарии.
- Старое поле `lessons.course_id` обнулено, выведено из употребления; дропнуть отдельной миграцией-чисткой позже.

### Дальше

1. Проверка пользователем RLS-укрепления (5 сценариев: edit-курс, edit-урок, публичный курс, каталог, /admin/lessons).
2. **Остатки по RLS-карте (отдельный шаг):** самопокупка в `purchases` (актуально к оплате), открытая запись в `analytics_events`/`likes`, 12 таблиц без политик (тесты/reviews/reports/subscriptions — неактивные фичи, заморожены), messages без DELETE-политики (кнопка удаления сообщения, вероятно, не работает), user_bans без политик (админские баны, вероятно, не работают).
3. Низкие находки dogfood #5–#9, восстановить `/dashboard/mentor`, кастомная not-found.
4. Потом: «запуск с минимумом» vs «достройка функций».

### RLS-укрепление 2026-09-02 (выполнено)

- Карта: 37 таблиц, RLS on везде кроме course_lessons; политики в основном дублированы (3–4 слоя от разных итераций).
- Исправлено скриптом `docs/migrations/2026-09-02-rls-hardening.sql`: lessons/lesson_content — владелец через coaches-join вместо «любой наставник» (критично: раньше любой наставник правил чужие уроки); публичное чтение только опубликованного (`is_published` или курс опубликован); course_lessons — RLS включён + политики; courses — снесена утечка черновиков (`courses_select_all`) и мёртвые политики (`auth.uid() = coach_id` — coach_id это id coaches, не auth); админам — `*_admin_all` через coaches.role='admin'. Deadlock при первом прогоне (dev-сервер держал таблицы) — прогнано при остановленном сервере.
- Проверено REST-уровнем: anon читает опубликованное, черновики скрыты, course_lessons публично только для опубликованных курсов.

## Сессия 2026-09-02 (часть 1) — dogfood-тест + фиксы находок + скиллы + промо-ролик

**Контекст:** сессия началась в промо-проекте видео, затем установка скиллов, затем dogfood-тест платформы и фиксы.

### Сделано

1. **Промо-ролик CoachPlatform — финал** (проект `D:\Projects\video\coach-promo-video`): фикс центрирования сцен (`.clip { width/height: 100% }`), новая фраза 1 («Эксперты отдают платформам до половины выручки»), ударения Silero через `+` («марк+етплейсов»), финальный рендер `--quality high` → `promo.mp4` (19 с, 3.6 MB). Голос xenia одобрен. **Принят пользователем.**
2. **Анализ скиллов hermes-agent** (~50 шт., 13 категорий) → таблица пользы для B2C/B2B/мобайл/игр в `D:\Projects\tmp\hermes-skills-analysis.md`.
3. **Установлены 8 скиллов в `~/.claude/skills/` (все с аудитом безопасности — чисто):** dogfood, requesting-code-review, claude-design, popular-web-designs (54 шаблона), humanizer, competitor-news-monitor, pdf, docx, powerpoint.
4. **Dogfood-тест платформы** (HTTP-уровень, curl + SSR-разбор; браузерных инструментов в среде нет) → отчёт `dogfood-output/report.md`: **9 находок** (0 критических, 1 высокая, 3 средних, 5 низких).
5. **Фиксы по отчёту:**
   - **#1 (высокая) ✅** — созданы `app/terms/page.tsx` и `app/privacy/page.tsx` (черновики-заготовки с пометками `[заполнить]`: реквизиты, e-mail, роль в расчётах); в футер `app/layout.tsx` добавлены ссылки на обе страницы.
   - **#3 — ложное срабатывание.** Кнопка «Регистрация» в Navbar уже есть; curl-тест не увидел клиентский рендер.
   - **#4 ✅** — «Купить курс»/«Купить урок» (`course/[id]`, `lesson/[id]`) больше не ссылаются на несуществующий `/checkout` (404!): неактивная кнопка + «Онлайн-оплата появится скоро».
   - Обновлены находки #1/#3/#4 в `dogfood-output/report.md`.

### Где остановились

- Пользователь не просматривал в браузере: `/terms`, `/privacy`, футер, нейтрализованные кнопки «Купить» (проверка — первый вход в следующую сессию).
- Следующий шаг: **находка #2 из dogfood-отчёта — серверная защита приватных страниц** (`/dashboard/mentor/*`, `/favorites`, `/notifications`, `/feedback`, `/messages`, `/mentor/analytics` сейчас отдают 200 анониму, редирект только клиентским JS; эталон — `/admin`, серверный 307). Варианты: проверка в `proxy.ts` по списку префиксов или server-layout с `getUser()`.
- Это же по сути **пункт 3 плана безопасности** (серверные проверки) — смержить с ним.

### Дальше (остатки фазы «безопасность + хотфиксы»)

1. ✅ RLS + проверка.
2. ✅ `proxy.ts` + `/auth/callback` (PKCE) — **не проверено пользователем на сайте** (логин держится на всех страницах; письмо восстановления пароля — опционально, может потребовать `http://localhost:3001` в Supabase → Redirect URLs).
3. Серверные проверки владельца в edit-страницах **+ dogfood-находка #2 (защита приватных страниц)** ← следующий.
4. Хотфиксы UX: ✅ «Купить» нейтрализованы, ✅ `/terms` + `/privacy`; осталось — восстановить `/dashboard/mentor`.
5. Низкие находки dogfood: #5 (`/profile/[id]` 404 — сверить с дублем `/mentor/[id]`), #6 (auth в messages layout), #7 (кастомная not-found), #8 (SSR главной), #9 (тестовый урок «тест картинки» в публичном курсе).
6. Потом: выбор «запуск с минимумом» vs «достройка функций».

### Открытые вопросы

- Бакеты Storage: конфликт `lesson-files` vs `lesson_files`; сверить список в Supabase Storage.
- Триггер `handle_new_user` — есть ли в БД.
- 8 таблиц с RLS-on и 0 политик — используются? решаем при чистке.
- Платёжный сервис не выбран (кнопки «Купить» ждут решения; след `prodamus_order_id`).
- `dogfood-output/` не коммитить (доказательства-HTML); удалить после закрытия находок.


## Сессия 2026-09-01 (2) — проверка RLS + фиксы комментариев и видео

### Сделано

1. **Проверка RLS-фикса на сайте — все 4 пункта пройдены:** аноним видит комментарии; залогиненный пишет; чужой комментарий — только «Жалоба»; аноним открывает курс/урок как раньше.
2. **Фикс: комментарии для анонима** (`components/LessonComments.tsx`) — загрузка комментариев запускалась только у залогиненных (`if (userId !== null)`), аноним вечно видел скелетон. Теперь комментарии грузятся всегда; проверки бана/отзыва — только для залогиненных.
3. **Фикс: видео-плееры по ссылке, а не по типу из базы** — тип `content_type` в БД часто неверный (`youtube`-ссылка с типом `video` → чёрный `<video>` + CORB-блокировка; VK-ссылка с типом `video` → ERR_TIMED_OUT).
   - Создан **реестр плееров `lib/video-embed.ts`**: youtube (watch/shorts/youtu.be), vk (vkvideo/vk.com/vk.ru), rutube, dzen. Правило «ссылка → embed-URL», новая платформа = одна запись.
   - `app/lesson/[id]/page.tsx`: плеер определяется по самой ссылке через `getVideoEmbedUrl()`; нераспознанные ссылки — прежний fallback («Открыть контент»).
   - Убран дубль `allowFullScreen` в iframe (предупреждение браузера).
4. **Убран шум в консоли:** `console.error` при отсутствии сессии в `Navbar.tsx` (аноним — норма, не ошибка).
5. Диагностика сети: у пользователя VPN; ошибки DNS/`ERR_NETWORK_CHANGED` — сеть, не код.

### Где остановились

- Проверить: ютуб-урок играет (после перезагрузки VPN), Rutube/Дзен — тестовый урок при желании.
- Следующий шаг фазы «безопасность + хотфиксы»: **пункт 2 — `proxy.ts` (middleware Next 16) + `/auth/callback` (PKCE)**.

### Дальше (план фазы «безопасность + хотфиксы»)

1. ✅ Ревизия RLS + проверка на сайте.
2. `proxy.ts` (middleware Next 16) + `/auth/callback` (PKCE) — сессии и вход. ← следующий
3. Серверные проверки владельца в `courses/[id]/edit` и `lessons/[id]/edit`.
4. Хотфиксы UX: восстановить `/dashboard/mentor` (из сироты `MentorDashboardClient.tsx`), нейтрализовать кнопки «Купить», страницы `/terms`, `/privacy`.
5. Потом: выбор «запуск с минимумом» vs «достройка функций».

## Сессия 2026-09-01 (1) — перенос проекта в Claude Code + аудит + RLS-фикс

**Контекст:** до этого проект вёлся Анатолием вручную через браузерный Qwen. Сессия прошла в Claude Code (папка сессии — matchself, все результаты — здесь, в coach-platform).

### Сделано

1. **Перенос настроек:**
   - Глобальные правила работы вынесены в `~/.claude/CLAUDE.md` (действуют во всех проектах).
   - `CLAUDE.md` в matchself сокращён до проектного; здесь — только стек и статус.
2. **Контекст проекта:** создан `PROJECT.md` (описание, статус, экраны, Supabase-слой — из интервью).
3. **Полный аудит** (4 параллельные проверки, read-only) → отчёт в `docs/audit-2026-09-01.md`:
   - Безопасность: фиктивные проверки владельца в edit-страницах, нет middleware/PKCE, fail-open модерации.
   - UX: `/dashboard/mentor` — 404 (11 ссылок), `/checkout`, `/terms`, `/privacy` не существуют.
   - Структура: ~100 КБ мёртвых компонентов, мусорные файлы, дубль профиля `/mentor/[id]` vs `/profile/[id]`.
   - Качество: tsc — 0 ошибок; ESLint — 276 проблем (117 `any`, паттерн load-функций).
4. **Решение №1** (PROJECT.md): сначала безопасность + хотфиксы (пункты 1–2 аудита), потом «запуск с минимумом» vs «достройка».
5. **Ревизия RLS** (SQL через SQL Editor Supabase):
   - Обнаружено: `comments` — RLS выключен + 0 политик (таблица из алерта Supabase от 17 Aug 2026); `purchases` — RLS выключен, 1 политика не действовала.
   - **Применино:** снесены 9 старых политик `comments` (включая «читать всё всем»), RLS включён, создан чистый набор из 4 политик (читать всем кроме приватных, писать/менять/удалять — только автору). `purchases`: RLS включён, добавлена `purchases_insert_own`. Скрипт выполнился: `Success. No rows returned`.

### Где остановились

- **Не сделана проверка RLS-фикса на сайте** (следующий шаг, 4 пункта):
  1. Страница урока — комментарии показываются.
  2. Залогиненный пользователь пишет комментарий — отправляется.
  3. Чужой комментарий изменить/удалить — не получается.
  4. Аноним: страница курса/урока открывается как раньше.
- Скрины отладки RLS в `C:\matchself\scrin_bag\` (3 шт.) — удалить после успешной проверки.

### Дальше (план фазы «безопасность + хотфиксы»)

1. ✅ Ревизия RLS (осталась проверка).
2. `proxy.ts` (middleware Next 16) + `/auth/callback` (PKCE) — сессии и вход.
3. Серверные проверки владельца в `courses/[id]/edit` и `lessons/[id]/edit`.
4. Хотфиксы UX: восстановить `/dashboard/mentor` (из сироты `MentorDashboardClient.tsx`), нейтрализовать кнопки «Купить», страницы `/terms`, `/privacy`.
5. Потом: выбор «запуск с минимумом» vs «достройка функций».

### Открытые вопросы

- Бакеты Storage: конфликт `lesson-files` vs `lesson_files`, бакет `uploads` — сверить фактический список в Supabase Storage.
- Триггер `handle_new_user` — есть ли в БД (создание profiles/coaches при регистрации).
- 8 таблиц с RLS-on и 0 политик (`tests`, `modules`, `views`, `test_results`, `test_questions`, `test_answers`, `comment_reports`, `user_comments`) — используются? решаем при чистке.
- Платёжный сервис не выбран (в `purchases` есть колонка `prodamus_order_id` — след ранней задумки, не решение).