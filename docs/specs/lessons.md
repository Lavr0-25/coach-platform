# Спека: уроки и их контент (`lessons` + `lesson_content`)

> Статус: `stable` · обновлено 2026-09-18 · ответственный: Анатолий + Claude Code

## Модель данных

- `lessons` — урок: title, description, price, is_free_preview, cover_image,
  is_published (черновик/опубликован), coach_id, is_hidden + link_access
  (см. «Скрытые уроки» ниже), publish_at (расписание публикации),
  published_at (фактический момент публикации — ставит ручная публикация
  и планировщик, null после снятия с публикации).
  Колонки `course_id` больше нет — связь урок↔курс через `course_lessons`.
  RLS `course_lessons_owner_all` (миграция
  `docs/migrations/2026-09-12-course-lessons-owner.sql`, исправлена миграцией
  `2026-09-12-fix-course-lessons-recursion.sql` тем же днём) разрешает автору
  добавлять в свой курс **только свои уроки**: проверяется владелец и курса,
  и урока. Раньше проверялся только владелец курса — чужой урок можно было
  прикрепить прямым REST-запросом (UI фильтровал «свои», сервер — нет; найдено
  обходом 12.09, в БД была живая связка урок Ивана → курс Дарины). Читают
  связку все (для опубликованных курсов — политика `course_lessons_public_read`).
  **Грабля (12.09):** первая версия политики проверяла владельца урока прямой
  выборкой из `lessons` → петля RLS (policy lessons → course_lessons → lessons)
  → у всех залогиненных запросы уроков падали с 42P17 «infinite recursion»
  (гость не страдал — его политика не смотрит lessons). Правило: если политика
  таблицы A читает таблицу B, чья политика читает A — проверку в политике A
  делать через SECURITY DEFINER-функцию (`is_coach_of_lesson`, RLS внутри неё
  не применяется). Верификация политики обязана включать пробу под токеном
  залогиненного, не только под anon.
- `lesson_content` — контент урока (1 запись на урок, order_index):
  - `content_type` — **CHECK-ограничение** в БД сохранено
    (`video | text | pdf | image | storage | other`), но платформа пишет
    только `text` (см. «Только текстовые уроки» ниже). CHECK не расширяли и
    не сужали: он страхует от мусора при прямых записях в БД.
  - `content_url` (NOT NULL) — для `text` — пустая строка.
  - `content_html` — HTML текстового урока.
  - `title` — подпись контента; для `text` не используется.
- Миграции: `docs/migrations/` (content_html + CHECK c 'text' — 2026-09-02).

## Лайки и избранное уроков (2026-09-05)

Две отдельные реакции на урок, иконки различаются:

- **Лайк** — сердечко (красное при активном), таблица `likes`.
- **Избранное** — звезда (янтарная при активной), таблица `favorites`
  (компонент `components/FavoriteButton.tsx`).

### Данные

- `likes` — user_id (nullable — дизайн-задел под анонимные лайки по IP,
  колонка `ip_address` не используется), lesson_id, created_at.
- RLS: SELECT всем («Anyone can view likes» — счётчик публичен), INSERT/DELETE
  только своих (`auth.uid() = user_id`); аноним вставить не может.
- **Unique-индекс** `likes_user_lesson_uniq (user_id, lesson_id) WHERE user_id IS NOT NULL`
  (миграция 2026-09-05) — один лайк на пользователя, двойной клик/гонка не
  создают дубликаты. Компонент при ошибке 23505 считает, что лайк уже стоит.
- `get_lesson_social_counts(p_lesson_ids uuid[])` — SECURITY DEFINER функция
  (миграция 2026-09-05): возвращает likes + favorites по списку уроков.
  Нужна, потому что RLS favorites не даёт автору читать чужие строки
  избранного. Фильтр `coaches.user_id = auth.uid()` — только по СВОИМ урокам;
  EXECUTE отозван у anon, выдан authenticated.

### UI

- **Страница урока** (`app/lesson/[id]/page.tsx`): сервер считает лайки
  (head-count) и «лайкнул ли я» — передаёт в `components/LikeButton.tsx`
  (client, toggle, оптимистичный счётчик; гость → `/login`; автор видит
  статичный счётчик, лайкнуть свой урок нельзя). Кнопка в ряду статистики
  рядом с избранным.
- **Кабинет автора** (`/dashboard/mentor/lessons`): на карточке каждого
  урока пилюли «❤ N» (лайки) и «⭐ N» (в избранном) — данные через
  `get_lesson_social_counts` (RPC).
- **Аналитика** (`/mentor/analytics`): две итоговые карточки
  («Лайков», «В избранном» — суммы по всем урокам) и колонка «Реакции»
  в таблице «Все уроки» (❤ N ⭐ N по каждому уроку) — те же данные
  через `get_lesson_social_counts`.

### Приёмочные критерии

- Кликом лайк ставится/снимается, счётчик меняется сразу, после F5 состояние сохраняется.
- Дублей в `likes` быть не может (unique-индекс).
- Гость при клике уходит на `/login`.
- Автор видит число лайков и избранных на карточках своих уроков, в т.ч. 0.

## Только текстовые уроки (2026-09-04)

Решение: на платформе один тип контента — текстовая статья в WYSIWYG.
Выбор типа из форм создания/редактирования убран:

- **UI (`/dashboard/mentor/lessons/new`)** — селектора «Тип контента» нет;
  карточка «Контент урока» — пояснение, что текст пишется в редакторе.
  Урок создаётся без строки `lesson_content`, автор ведётся сразу в
  `/[id]/edit` (как раньше для `text`).
- **UI (`/dashboard/mentor/lessons/[id]/edit`)** — в карточке «Контент урока»
  сразу WYSIWYG-редактор, без выбора типа, поля ссылки и загрузчика файла.
  «Сохранить изменения» шлёт `content_type='text'`, `content_url=''`.
- **БД** — все уроки конвертированы в `text` (2026-09-03, SQL-конверсия,
  10/10); не-текстовых строк нет. Логика чтения старых типов в коде
  (embed и т.п.) не поддерживается формами и не актуализируется.

## Текстовый урок (WYSIWYG, Tiptap 3)

### Редактор (наставник)

- `components/editor/RichTextEditor.tsx`, расширения — **один источник**:
  `lib/editor/lessonExtensions.ts` (StarterKit, Image, Youtube, TextAlign, VkVideo).
  Этот же набор используют очистка и вывод — расхождение невозможно.
- Блоки: H1–H3, Ж/К/Ч, маркерованный/нумерованный список, цитата, разделитель,
  выравнивание (лево/центр/право — для абзацев и заголовков), картинки
  (Storage `lesson_files`, ≤10 МБ, всегда по центру), YouTube, VK.
- VK — своё расширение `VkVideo` (готового нет): `vk.com|vkvideo.ru/video-oid_id`
  → embed `video_ext.php?oid=...&id=...`.
- Панель закреплена под шапкой (sticky top-16). Контейнер редактора — БЕЗ
  `overflow-hidden` (он превращает sticky в «липание внутри контейнера»).
- Создание урока (`/new`): урок создаётся без контента, наставник
  ведётся сразу в редактор `/[id]/edit`; строка `lesson_content` появляется
  при первом сохранении.

### Очистка HTML (безопасность, главная часть фичи)

- `lib/editor/sanitizeLessonHtml.ts` — только для сервера (`@tiptap/html/server`,
  требует happy-dom). Идея: сам Tiptap — санитайзер. `generateJSON` парсит HTML
  через схему — всё вне схемы отбрасывается; `generateHTML` собирает чистый HTML.
- Применяется **дважды**: при сохранении (server action `updateLesson`) и при
  выводе (`app/lesson/[id]`, server component) — двойной замок.
- Проверено тестом (2026-09-02): `<script>`, `onclick`/`onerror`, `href="javascript:"`,
  чужие `<iframe>` — удаляются; h1/strong/img — сохраняются.
- `content_url` для `text` всегда `''`; пустой текст (`<p></p>`) → ошибка
  «Текст урока пуст», не сохраняется.

### Просмотр (ученик)

- `app/lesson/[id]` → `content_type='text'` → `<article class="lesson-prose">`
  с очищенным HTML (`dangerouslySetInnerHTML` — безопасно, т.к. выше два слоя очистки).
- Стили `.lesson-prose` / `.lesson-prose-editor` в `globals.css` — только на
  токенах темы (`--t-ink-*`, `--brand`): перекрашиваются в тёмной теме и палитрах.
  Мобильная типографика — media 640px.

## Приёмочные критерии (фича WYSIWYG, 2026-09-02)

1. Наставник создаёт урок типа «Текст», пишет с форматированием, вставляет
   картинку и YouTube-ссылку → сохраняется. ✅ (проверено Анатолием, после фикса CHECK)
2. Ученик видит отформатированный текст, в т.ч. на мобильном и в тёмной теме.
3. В сохранённом HTML нет скриптов/обработчиков — ✅ (тест очистки выше).
4. Бесплатное превью/цена работают как для остальных типов.
5. `tsc` — 0 ошибок; спека обновлена в наборе правок.

## Границы / открытые вопросы

- Ошибки сохранения: плашка вверху формы + авто-прокрутка наверх (добавлено,
  т.к. на длинной странице ошибку не было видно).
- Консольные ошибки со страницы урока с VK-вставкой (CORS apptracer.ru, 403/406
  stats.vk-portal.net) — шум самого VK-плеера внутри его iframe, к платформе
  не относятся, не чиним.
- Зависимости: @tiptap/react, starter-kit, extension-image, extension-youtube,
  extension-text-align, @tiptap/html + happy-dom (сервер). Редактор ~150 КБ JS
  только на странице редактирования.

## Публикация без контента запрещена (2026-09-03)

Урок нельзя опубликовать без сохранённого контента (раньше студент видел
заглушку «Контент урока недоступен» — из docs/backlog.md, закрыто):

- **Сервер** (`setLessonPublished` в app/actions/updateLesson.ts): при
  `published=true` проверяется `lesson_content` — для `content_type='text'`
  нужен непустой текст вне HTML-тегов, для остальных типов — непустая
  `content_url`. Пусто/нет записи → ошибка «Сначала заполните и сохраните
  контент урока…». Проверка на сервере — главная (UI не доверенный).
- **UI** (страница редактирования урока): у черновика без сохранённого
  контента кнопка «Опубликовать урок» неактивна (`disabled`), в статусе
  черновика видна подсказка «…сначала заполните и сохраните контент».
  Флаг `hasSavedContent` ставится при загрузке урока (по тем же правилам
  непустоты) и после успешного «Сохранить изменения».
- Снятие с публикации не ограничивается — «Вернуть в черновик» работает всегда.

## Публикация по расписанию (2026-09-03)

Автор задаёт уроку момент публикации (`lessons.publish_at`, timestamptz); открыть
урок в срок может и без участия автора:

- **Планировщик в БД** — pg_cron, job `publish-scheduled-lessons` каждую минуту:
  `update lessons set is_published = true, published_at = now() where publish_at is not null and is_published = false and publish_at <= now()` +
  проверка непустого контента через `lesson_content` (то же правило непустоты, что
  у ручной публикации). Job работает от postgres, RLS не применяет — меняет только
  `is_published`/`published_at`/`updated_at` по служебному условию. Миграции:
  `docs/migrations/2026-09-03-scheduled-publish.sql`,
  `docs/migrations/2026-09-05-lessons-published-at.sql`.
- **UI (страница редактирования):** рядом с публикацией есть «🗓 По расписанию» —
  панель с `datetime-local`. Доступна и черновику (с сохранённым контентом), и
  опубликованному уроку: у опубликованного установка расписания **сразу снимает
  его с публикации** (вариант А), планировщик откроет урок в назначенное время.
  Запланированное состояние: заголовок статуса «Публикация запланирована: …» +
  кнопка «Отменить расписание». Ручная публикация перекрывает расписание
  (`publish_at = null` при `setLessonPublished(published=true)`). Снятие с
  публикации тоже чистит `publish_at` — иначе старое расписание в прошлом снова
  опубликует урок планировщиком сразу после «Вернуть в черновик».
- **UI (список «Мои уроки»):** у черновика с расписанием — бейдж
  «🗓 дата-время» + двухшаговая отмена («Точно отменить?») прямо в карточке;
  у опубликованного — бейдж «📅 дата публикации» (колонка `published_at`).
- **Сервер** (`setLessonPublishAt` в app/actions/updateLesson.ts): свой урок,
  время в будущем, контент непустой (правило «публикация без контента
  запрещена» выше); `null` отменяет расписание. Установка на опубликованном
  уроке: `is_published = false, published_at = null` в том же update.
- Урок с расписанием до наступления срока ведёт себя как черновик: 404 для всех,
  кроме автора; в каталоге и sitemap отсутствует. Точность публикации — до минуты.

## Удаление урока наставником (2026-09-03)

Наставник может удалить свой урок двумя путями:
- кнопка «Удалить урок» внизу страницы редактирования (красная, справа от
  «Отмена»), подтверждение через confirm с предупреждением, если урок
  опубликован; после успеха — редирект в «Мои уроки»;
- корзина в правом верхнем углу карточки в списке «Мои уроки»
  (`/dashboard/mentor/lessons`, 2026-09-03) — двухшаговое подтверждение
  («Точно удалить?»), тост об успехе, карточка уходит из списка без перезагрузки.

- **Сервер** (`deleteLesson` в app/actions/deleteLesson.ts): удаляет строку
  `lessons` только при совпадении `coach_id` (проверка владельца + RLS
  `lessons_owner_delete`). Связанные записи чистит база: все 13 дочерних
  таблиц (lesson_content, lesson_progress, comments, likes, favorites,
  purchases, course_lessons, tests и др.) висят на `ON DELETE CASCADE`,
  каскады выполняются от владельца таблицы и RLS дочерних таблиц не
  применяются.
- **Урок с покупками автор удалить НЕ может** (решение Анатолия, 2026-09-03):
  `deleteLesson` проверяет `purchases` по `lesson_id` — если есть покупки,
  возвращает ошибку «Урок уже купили — … удаление купленных уроков делает
  только администрация». Причина: каскад стёр бы записи о деньгах.
- **Купленный урок удаляет только админ** — `deleteLesson` в
  `app/admin/actions.ts` не имеет проверки покупок: удаляет `lesson_content`,
  затем `lessons`; покупки уходят каскадом (история оплат по уроку
  не сохраняется — осознанный компромисс до появления архивации покупок).
- **Файлы Storage не удаляются** (обложка, PDF/картинки контента) — остаются
  осиротевшие объекты бакетов; чистка отдельной задачей (бэклог при росте
  объёма).

## Скрытые уроки (2026-09-04)

Режим урока «вне каталога»: не виден в каталоге, поиске, sitemap и профиле автора;
открывается только тем, кого автор допустил. Приватный канал распространения
(ссылка в мессенджер, закрытая группа учеников и т.п.). Миграции:
`docs/migrations/2026-09-04-hidden-lessons.sql` (включая фикс рекурсии RLS — раздел 5).

### Режимы и данные

- `lessons.is_hidden` (default false) — скрытый режим; `lessons.link_access`
  (default true) — приём по ссылке.
- `lesson_access` — допуски: unique(lesson_id, user_id), `source` = manual/link,
  `revoked` (отзыв, строка не удаляется — иначе ссылка вернула бы доступ),
  `added_by`. RLS включён.
- Скрытый урок всегда самостоятельный: при включении режима серверный экшен
  отвязывает урок от всех курсов (`course_lessons`); автор может отвязать от
  чужого курса (RLS-политика `course_lessons_lesson_coach_delete`).
- Скрытый урок публикуется (`is_published=true`) при включении режима.

### Кто видит скрытый урок (RLS)

- Автор (coaches.user_id) — всегда; допущенные (`lesson_access`, revoked=false).
- Политики проверки вынесены в SECURITY DEFINER-функции
  `is_coach_of_lesson()` / `lesson_accepts_link()` — прямые подзапросы к lessons
  из политик lesson_access давали «infinite recursion» и 500 на всех уроках.
- Все прочие (включая анонима) — RLS не отдаёт урок → 404.

### Доступ (страница урока `app/lesson/[id]`)

- **По ссылке** (`link_access=true`): залогиненный, ещё не допущенный,
  автоматически добавляется (`ensureLinkAccess` — до чтения урока; upsert
  `source='link'` с `ignoreDuplicates`). Отозванный снова не попадает:
  строка с revoked=true блокирует и чтение, и повторный самозахват (unique).
- **Только приглашения** (`link_access=false`): ссылка не открывает — не
  допущенный получает 404; допущенные видят как обычно.
- **Аноним**: если урок существует → редирект на `/login?next=/lesson/<id>`
  (после входа — возврат на урок; `?next=` принимает только внутренние пути).
  Несуществующий id — обычная 404.
- **Оплата обязательна** (решение Анатолия): допуск даёт право видеть страницу
  урока (шапка, описание, комментарии); контент — по общим правилам (цена/
  покупка/бесплатное превью). Скрытие не отменяет цену.
- Концепты, объясняющие код: `ensureLinkAccess` вызывается и в `generateMetadata`
  (метаданные и страница рендерятся параллельно, у каждого свой запрос урока);
  перечитывание после первого запроса не работает — Next.js мемоизирует fetch
  внутри рендера, поэтому вставка делается ДО чтения, а в метаданных перечитывание
  меняет URL (`.limit(1)`).

### Исключение из публичных списков (фильтр `is_hidden` в коде)

- Главная (catalog ленты, `app/page.tsx`), sitemap (`app/sitemap.ts`),
  публичный профиль автора (`components/MentorProfile.tsx` — список и счётчик
  «N уроков»), каталог авторов `/mentors` (счётчики уроков).
- Кабинет автора, админка, избранное/уведомления — скрытые уроки видны
  (это приватные зоны; у автора панель управления, у админа — обзор).

### UI кабинета (`components/HiddenLessonPanel.tsx`)

- Панель внизу страницы редактирования (после «Цена и доступ»); в скрытом
  режиме наверху компактный статус «🔒 Урок скрыт». Тумблеры (role="switch"):
  «Скрытый режим» и «Приём по ссылке» + кнопка «Скопировать ссылку».
- Поиск людей для приглашения: профили (имя+email) и публичное имя автора
  (coaches.display_name), слияние без дублей; публичное имя показывается первым.
- Список допущенных: аватар, имя, email, пометка «по ссылке»/«вручную»,
  «Отозвать»; свёрнутый блок «Отозванные: N».
- Серверные экшены `app/actions/hiddenLesson.ts`: setLessonHidden (проверка
  контента, отвязка от курсов, is_published=true), setLessonLinkAccess,
  addLessonAccess (восстанавливает отозванную строку — это ручное приглашение),
  revokeLessonAccess (revoked=true).

### Приёмочные критерии

1. Аноним по ссылке → `/login?next=…`; после входа урок открывается. ✅ (ui-test 2026-09-04, 9/9)
2. Не допущенный залогиненный — 404; допущенный по ссылке — видит, пометка
   «по ссылке» в панели автора. ✅
3. Отозванный — 404, повторный заход по ссылке доступ не возвращает. ✅
4. Режим «только приглашения» (link_access=false): ссылка не открывает (404),
   допущенные видят. ✅
5. Скрытого урока нет на главной, в sitemap, в профиле автора и в счётчиках /mentors.
6. Мобильный вид — без горизонтального скролла. ✅

### Границы / известные особенности

- ~~Комментарии скрытого урока читались без проверки видимости урока~~ —
  **закрыто 2026-09-04**: политики SELECT/INSERT таблицы `comments` проверяют
  `lesson_visible_to()` (виден ли урок: не-скрытый — да, скрытый — автору или
  допущенному). Миграция: `docs/migrations/2026-09-04-comments-hidden-lesson.sql`.
  Проверено: анонимный REST-запрос к комментариям скрытого урока — 0 строк,
  обычные уроки не задеты. UPDATE/DELETE комментариев остались «только свой»
  (утечки контента через них нет). Комментарии черновиков (не-скрытых,
  неопубликованных) по-прежнему читаются знающим id — узкий случай, граница
  не двигалась.
- **Мат-фильтр комментариев на сервере** (миграция
  `docs/migrations/2026-09-12-security-messages-profiles.sql`; логика
  заменена `docs/migrations/2026-09-16-banned-words-whole-word.sql`): триггер
  `comments_banned_words` (INSERT/UPDATE) проверяет `content` по `banned_words`
  и отклоняет запись с матом. С 16.09 сравнение — **по целым словам** (текст
  режется на токены): поиск подстроки отклонял честные тексты («бля» в
  «рубля», «манда» в «команда», «конча» в «кончается»). До 12.09 фильтр был
  только на клиенте — через прямой REST-запрос мат проходил (найдено
  пентестом 12.09). Аналогичные триггеры стоят на `feedback` (спека feedback)
  и `reviews` (отзывы — отдельной спеки нет); клиентский фильтр
  (`lib/banned-words.ts`) — та же логика целых слов, дружелюбный первый барьер.
- Мета-теги скрытого урока не отдаются поисковикам (для них урок — 404).
- Скрытый урок не может жить в курсе (отвязка при включении режима).

## Подсчёт просмотров и источник перехода (2026-09-16, №30)

> Статус раздела: `stable`

Серверный рендер страницы урока (`app/lesson/[id]/page.tsx`) пишет событие
`lesson_view` в `analytics_events` — **в том числе для гостей** (не
авторизованных читателей бесплатного контента):

- `user_id: null` для гостя, `metadata.source` — источник перехода (jsonb,
  отдельной миграции не потребовалось — колонка уже была).
- **Фильтр ботов**: типовые User-Agent краулеров (`bot|crawl|spider|slurp|
  headless|lighthouse|preview`) просмотры не создают — иначе охваты
  раздувались поисковиками.
- **Источник перехода** (`lib/utm.ts`, `resolveSource`): сначала
  `utm_source` из URL (наши ссылки с метками), затем referer-заголовок
  (органический Яндекс/Дзен/TG/VK/vc.ru/Google), иначе `direct`.
- **Правило utm-меток** (постоянное, 16.09): все внешние ссылки и кнопки
  «Поделиться» платформы несут `utm_source` (и при необходимости
  `utm_campaign`), чтобы в аналитике строились воронки каналов.
  Хелпер — `withUtm()` в `lib/utm.ts`.
- Чтение событий: RLS «Mentors can view own analytics» отдаёт автору и
  гостевые события его уроков. Вставка гостей — политика
  `analytics_anon_insert_views` (anon, только profile_view/lesson_view).
- Отображение: секция «Динамика охватов» в `/mentor/analytics` — карточка
  на материал: SVG-спарклайн просмотров по дням (30 дней, без
  зависимостей), итог за период, чипы источников (`sourceLabel`).
  Все графики нормализованы к общему max — карточки сопоставимы.
- **Детальная страница материала** `/mentor/analytics/[id]` (клик по
  «Охватам» в таблице или по карточке охватов): период 7/30/90 дней,
  график по дням (просмотры + покупки), сводка за период (охват,
  покупки, «на руки», конверсия), источники переходов, вовлечённость
  (начали читать/завершили/дочитали из lesson_progress; для курса —
  агрегат по его урокам). Доступ: только владелец-ментор (проверка
  coach_id при загрузке, RLS вторым слоем).
- **Курсы в охватах**: событие `course_view` теперь пишется и гостями
  (миграция 2026-09-16-course-view-anon.sql расширила две политики:
  `analytics_anon_insert_views` — course_view от анонима; «Mentors can
  view own analytics» — ветка course для событий своих курсов). Страница
  курса трекает по той же схеме, что урок: metadata.source, фильтр ботов.
  Охват курса считается по course_view (визиты страницы курса), а не по
  просмотрам уроков курса — без двойного счёта.
- Таблица «Все материалы» в `/mentor/analytics`: уроки и курсы вместе,
  колонка «Охваты» (30 дней) с переходом на детальную страницу. Колонки:
  Материал / Охваты / Всего / За месяц / За день / Лайк / В избранном /
  Цена / Покупок / На руки (доход автора = сумма coach_earnings по completed-
  покупкам материала). Числовые колонки сортируются кликом по заголовку
  (desc → asc → сброс к порядку «свежие сверху»); сортировка — только на
  десктопе (заголовки скрыты на мобильных). Масштабирование
  на много материалов: поиск по названию, фильтры-чипы по числовым
  показателям («Все» / «С охватами» / «Читали» / «С продажами»), порционная
  выдача по 10 строк с кнопкой «Показать ещё» (смена поиска/фильтра
  сбрасывает порцию). Секция «Динамика охватов» показывает только топ-3
  материала по просмотрам за 30 дней.
- Детальная статистика материала `/mentor/analytics/[id]` (переход по клику
  на «Охваты»): период 7/30/90 дней, сводка за период (охват, покупки,
  «на руки», конверсия), график «По дням за период» (просмотры + покупки),
  график «Реакции по дням» (лайки и избранное — SQL-функция
  `get_social_series` (security definer, миграция
  `2026-09-16-social-series.sql`): RLS `favorites` не даёт автору читать
  чужие строки даже на своих материалах; функция проверяет владельца
  внутри через `coaches.user_id = auth.uid()`; у уроков — лайки и
  избранное, у курсов — только избранное по `course_id`), источники переходов
  за период — столбцы с накоплением по дням (цвет = источник, из
  metadata.source событий lesson_view/course_view), вовлечённость —
  график по дням из `lesson_progress` (started_at → «начали»,
  completed_at → «завершили»; итоги за всё время — плитки под графиком)
  + соцсчётчики. Все графики (включая «Динамику охватов»
  и «Активность» главной страницы) используют строку-подпись над графиком,
  обновляемую при наведении на день (hover-состояние в React), — вместо
  всплывающих тултипов, которые обрезаются контейнером с горизонтальной
  прокруткой.
- Гостевые просмотры учитываются с 16.09 — до этой даты охваты в данных
  отсутствуют (в UI это указано).
- **Терминология денег (16.09)**: два термина во всём блоке аналитики —
  «Оплачено» = сумма покупателя до вычета комиссии (колонка таблицы продаж,
  «Оплачено за период» на детальной), «На руки» = доход автора после комиссии
  (`coach_earnings`: карточки «Всего на руки» / «На руки за 30 дней», колонки
  таблиц продаж и материалов). Третья формулировка «Итого» из таблицы
  материалов убрана как источник путаницы.

## Показы и CTR каталога (2026-09-17)

> **Статус:** stable · **Обновлено:** 2026-09-18 · **Ответственный:** агент (Гоша)

Карточки материалов на главной трекингуют показы и клики — считается CTR
(кликабельность карточки каталога).

### События

Таблица `analytics_events` — схема не меняется, добавлены значения типов,
RLS-политика и расширение двух CHECK (`docs/migrations/2026-09-17-catalog-events-rls.sql`):

- `catalog_impression` — карточка попала в видимую область экрана
  (IntersectionObserver, порог 0.5 — половина карточки). Пишет клиентский
  `CatalogImpression` (`components/CatalogTracking.tsx`, обёртка карточек в
  `HomeFeed`). Один раз за сессию браузера на карточку (метки в
  `sessionStorage`, ключ `type:id`).
- `catalog_click` — клик по карточке до перехода (`trackCatalogClick`,
  fire-and-forget; переход SPA — запрос успевает уйти).
- Оба пишутся с привязкой к пользователю: у залогиненного `user_id = auth.uid()`
  (проходит политику `analytics_insert_own`), у гостя `user_id = null`
  (`components/CatalogTracking.tsx` читает `auth.getUser()` перед вставкой;
  до 18.09 слалось всегда `null` — RLS молча отклонял события залогиненных,
  403-шум в консоли). `target_type` = `lesson`/`course`,
  `metadata.source = 'catalog'`. Боты отфильтрованы на клиенте (`isBot`).

### Политики и ограничения

- RLS `analytics_anon_insert_catalog`: анониму разрешена вставка только
  `catalog_impression`/`catalog_click` с `user_id IS NULL`.
- CHECK `analytics_events_event_type_check` расширен: +
  `catalog_impression`, `catalog_click`.
- CHECK `analytics_events_target_type_check` расширен: + `course`.

### Определения

- **Показы** — число `catalog_impression` за фикс. окно 30 дней.
- **CTR** = клики / показы; при нуле показов — прочерк (не 0%).

### Отображение

- Общая аналитика, вкладка «Материалы»: группа колонок «Каталог»
  («Показы», «CTR»), сортируемые.
- Детальная страница материала: 5-я KPI-карточка «Показов в каталоге ·
  CTR x%» (CTR виден всегда, при нуле показов — прочерк).
- Отдельно от `lesson_view`: клики из каталога не смешиваются с переходами
  по внешним ссылкам (ТГ/Дзен) и не раздувают охваты страниц. Все чтения
  `analytics_events` в аналитике фильтруются по типу: общая страница —
  цикл с веткой каталога и `continue` для прочих типов, детальная —
  `.eq('event_type', 'lesson_view')`. Без фильтра новые типы событий
  раздували бы охваты (закрыто в этом же наборе).

### Подсказки интерфейса (2026-09-17)

Компонент `components/Hint.tsx` (иконка «i», подсказка по hover/focus —
работает на тач-экранах и с клавиатуры) добавлен к KPI-плашкам, заголовкам
графиков и колонкам таблицы на обеих страницах аналитики (общая +
детальная). Текст подсказки объясняет, что считается и чем метрика полезна.

## Статистика «Поделиться» (2026-09-18)

> **Статус:** stable · **Обновлено:** 2026-09-18 · **Ответственный:** агент (Гоша)

Считаем, сколько раз делятся материалами/автором/партнёрской ссылкой и сколько
читателей реально пришло по поделенным ссылкам.

### События

Таблица `analytics_events` (`docs/migrations/2026-09-18-referral-visit.sql`):

- `share` — нажатие «Поделиться» (`components/ShareButton.tsx`). Пишется при
  системной шторке (`channel: 'native'`) или копировании ссылки
  (`channel: 'copy'`); закрытие шторки пользователем не считается.
  `target_type` = `lesson`/`course`/`profile`, `user_id` — автор сессии
  или `null` (гость). `metadata.kind = 'referral'` — шаринг партнёрской
  ссылки (`app/dashboard/mentor/partner`), отличает её от обычного шаринга
  профиля.
- `referral_visit` — переход по реферальной ссылке `/?ref=<coach_user_id>`
  (`components/RefCapture.tsx`). `target_id` = код автора (совпадает с
  `auth.uid()` автора — SELECT-политика отдаёт событие ему как своё),
  `target_type = 'profile'` (CHECK target_type не расширялся). Один раз за
  сессию браузера на код (метка в `sessionStorage`). Регистрирует сам факт
  входа — независимо от того, зарегистрируется ли гость.
- `profile_view` — открытие страницы автора (`app/mentor/[id]`); тип был
  зарезервирован в CHECK/RLS с 2026-09-03, пишется с 18.09. Боты отфильтрованы
  (`isBot`).

### Определения

- **Поделились** — число `share` за период.
- **Переход** — открытие страницы с `metadata.source = 'share'`
  (`lesson_view`/`course_view`/`profile_view`; все поделеные ссылки идут с
  utm_source=share, `lib/utm.ts`) **или** `referral_visit` (партнёрская
  ссылка — сам вход по `?ref=`).
- **Зарегистрировались** (только партнёрка) — строки `referral_registrations`
  за период (миграция 2026-09-17, пишутся при первом входе приглашённого).
- **Переходов на одно «Поделиться»** = переходы / шары; прочерк при нуле.

### Отображение

- Автор, «Обзор» (`app/mentor/analytics`): плитки «Поделились» и «Переходов
  по ссылкам» с дельтой к прошлому периоду; в таблице «Материалы» — группа
  колонок «Поделиться» («Поделились», «Переходы»), сортируемые.
- Автор, вкладка «Поделиться» (2026-09-18, `?tab=share`): 4 KPI (поделились,
  переходы, переходов на одно «Поделиться», «Зарегистрировались» по
  партнёрской ссылке из `referral_registrations.coach_user_id` — автор читает
  свои строки по RLS), разбивка по типам (уроки, курсы, страница автора,
  партнёрская ссылка — «поделились · перешли»), динамика по дням (шары и
  переходы), топ-10 материалов автора со ссылками на детальную статистику.
  Переходы курсов считаются по `course_view` с `source='share'` (события
  `course_view` в охваты/просмотры уроков не попадают).
- Админ, `/admin/analytics` — «Аналитика площадки» (2026-09-18): полный
  аналог авторской аналитики, но по всем авторам и материалам сразу.
  Серверный компонент, период 7/30/90 дней (`?p=`), 4 вкладки (`?tab=`):
  - **Обзор** — деньги (оборот, на руки авторам, комиссия, покупок) и
    трафик (просмотры включая гостей, конверсия в покупку, поделились,
    переходов по ссылкам); график «просмотры и продажи по дням» (статичный
    SVG: линия + столбики); источники переходов с долями (метки utm);
    лучшие материалы топ-3 с источниками; «когда читают» (по дням недели).
  - **Материалы** — таблица всех уроков и курсов всех авторов: материал
    (название, тип, автор, дата), охваты30, показы/CTR каталога, открытия
    учеников (всего/за месяц), поделились/переходы, лайки, цена, покупок,
    оборот. min-w + горизонтальная прокрутка внутри карточки.
  - **Поделиться** — 4 KPI, разбивка «По типам» (включая «Зарегистрировались»
    из `referral_registrations`), динамика по дням, топ-10.
  - **Продажи** — итоги за период и всё время (оплачено / на руки / комиссия)
    + последние 15 продаж: дата, покупатель, материал (или «Подписка на
    автора · N мес.»), автор, оплачено, на руки. В деньгах только
    `completed` (purchases, subscription_payments); «на руки» = coach_earnings.
  Данные — сервисным клиентом (`createAdminClient`), гейт роли —
  в `app/admin/layout.tsx` и на самой странице.
- Границы: «переход» не привязан к конкретному «поделиться» (каналы
  копируются обезличенно), поэтому связь — по источнику, не по клику.

## История

- 2026-09-18 — usability-прогон, пакет правок: catalog-события пишут
  `user_id` залогиненного (был всегда `null` → 403-шум и потеря метрик);
  чип «Материалы» и хлебные крошки → «Уроки»; заглушки обложек карточек —
  5 вариантов градиента, детерминированно по id (`coverPlaceholderClass`);
  в конце урока блок «Что дальше» (до 3 других опубликованных материалов
  автора + гостю CTA «Войдите/зарегистрируйтесь»); плитка «Конверсия
  просмотр → покупка» переименована в «Конверсия в покупку» (обрезалась
  в узкой карточке).
- 2026-09-17 — подсказки «что это и зачем» (Hint) на плашках, графиках и
  колонках обеих страниц аналитики; CTR на 5-й карточке детальной виден
  всегда (прочерк при нуле показов).
- 2026-09-17 — CTR каталога: события `catalog_impression`/`catalog_click`
  (обёртка карточек главной), группа колонок «Каталог» в таблице материалов,
  пятая KPI-карточка на детальной; чтения `analytics_events` отфильтрованы
  по `event_type` (каталог больше не попадает в охваты).
- 2026-09-04 — защита комментариев скрытого урока: функция `lesson_visible_to()`,
  политики SELECT/INSERT `comments` проверяют видимость урока (закрытие границы
  фичи «Скрытые уроки»).
- 2026-09-04 — фича «Скрытые уроки»: is_hidden/link_access, таблица lesson_access
  с RLS, автодопуск по ссылке, панель управления в кабинете, исключение из
  публичных списков; фикс рекурсии RLS через SECURITY DEFINER-функции.
- 2026-09-04 — «только текстовые уроки»: выбор типа контента убран из форм
  создания/редактирования, `content_type` всегда `text`.
- 2026-09-05 — расписание для опубликованных уроков (снимает с публикации сразу,
  планировщик откроет в срок) + колонка `published_at`: бейдж «📅 дата публикации»
  в списке уроков, дата в статусе урока, снятие с публикации чистит `publish_at`
  (фикс: старое расписание в прошлом повторно публиковало урок).
- 2026-09-03 — публикация по расписанию: колонка `publish_at`, pg_cron-job каждую
  минуту, панель расписания на странице урока, бейдж с отменой в списке уроков.

- 2026-09-02 — фича «текстовый урок» (WYSIWYG Tiptap): колонка content_html,
  CHECK + 'text', редактор, VK-расширение, очистка двойным проходом, просмотр,
  sticky-панель, выравнивание.