# Бэклог проекта — coach-platform

> Статус: `stable` · обновлено 2026-09-02 · ответственный: Анатолий + Claude Code
>
> Правило: идея попадает сюда после согласования Анатолием (своя или из обращений
> пользователей через `/admin/feedback` → агентское API). Приоритет и порядок —
> решает Анатолий; агент предлагает рекомендации.

## 🔧 В работе

| Идея | Источник | Что это |
|---|---|---|
| **WYSIWYG-редактор текстовых уроков «как в Дзене»** | обращение пользователя (в работе) | Редактор текстовых уроков: жирный/курсив, картинки, вставки, видео — как в Дзене. Следующая большая тема |

## 📋 Согласовано, ждёт очереди

| Идея | Источник | Заметки |
|---|---|---|
| **Ответ пользователям на обращения + возврат на доработку** | Анатолий, 2026-09-02 | Поля `admin_reply` + `replied_at` в feedback; ответ виден в «Мои обращения»; статус можно вернуть в «Новое» с комментарием «недостаточно данных»; API PATCH принимает `reply` |
| **Индексация платформы для ИИ-агентов** | обращение пользователя | llms.txt, SSR главной, мета-теги — чтобы GenAI-агенты находили и рекомендовали курсы платформы |
| **Добавление уроков автоматически через ИИ** | обращение пользователя | Агент создаёт/редактирует уроки через API; у Анатолия «ИИ-завод контента». Развитие агентского API (ступень 2+) |

## 🗺️ Дорожная карта (наши решения)

| Тема | Заметки |
|---|---|
| **П4: ui-компоненты** | Toast/Button/Card/Badge/Input, lucide-react вместо emoji-иконок, слияние 3 загрузчиков файлов, перенос админских записей в server actions |
| **Ступень 2 модерации** | Методика `docs/specs/moderation.md` («рекомендую бан» → потом «бан с отчётом»), endpoint /api/agent/bans |
| **Агент по расписанию** | Настоящий cron-робот (GitHub Actions по таймеру + агентское API): сам читает новые обращения/жалобы, готовит сводку |
| **Язык интерфейса (этап 3)** | Переключение ru/en — этап 3 из плана настроек интерфейса |
| **Платежи** | Сервис не выбран (в purchases есть след задумки `prodamus_order_id`); решение отложить до запуска |

## 🧹 Технический долг

| Тема | Заметки |
|---|---|
| **Аудит RLS-политик всех таблиц** | Найдено на feedback: у пользователя не было UPDATE/DELETE своих строк (найдено 2026-09-02, исправлено). Проверить rest: reviews, comments, notifications, favorites и др. |
| ~30 `.single()` по проекту | При пустом результате кидают ошибку — заменить на безопасные запросы |
| Широкая SELECT-политика `storage.objects` | Сузить права на бакеты Storage |
| SSR главной страницы | Сейчас главная рендерится на клиенте — медленный первый кадр, плохо для индексации |
| **Перенос данных в РФ (152-ФЗ)** | Локализация персональных данных: выбор хостинга/БД в РФ, экспорт из Supabase, схема миграции. Вернуться при решении о переезде |

## ✅ Сделано (для памяти, без деталей)

- 2026-09-02 — цикл обращений: «Мои обращения», правка/удаление пока «Новое», массовые статусы, JSON-выгрузка
- 2026-09-02 — агентское API: ключи в базе (`/api-keys`), развилка по роли, жалобы со сводкой
- 2026-09-02 — фиксы: форма фидбека больше не блокируется после отправки (можно отправить следующее)